import React from 'react';

import Heading from '../Components/Heading';

const Sync = () => {
    return (
        <div>
            <Heading value='Sync'/>
        </div>
    );
}

export default Sync;